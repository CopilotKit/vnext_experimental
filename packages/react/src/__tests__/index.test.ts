import * as CopilotKitReact from "../index";

describe("@copilotkit/react", () => {
  it("should export the expected modules", () => {
    // Basic test to ensure the package loads correctly
    expect(typeof CopilotKitReact).toBe("object");
  });
});
